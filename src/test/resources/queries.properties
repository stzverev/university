students.select = \
    SELECT \
        students.id AS student_id, \
        students.first_name AS student_first_name, \
        students.last_name AS student_last_name, \
        groups.id AS group_id, \
        groups.name AS group_name \
    FROM students AS students \
        LEFT JOIN groups AS groups \
        ON students.group_id = groups.id
        
students.select.byId = \
    SELECT \
        students.id AS student_id, \
        students.first_name AS student_first_name, \
        students.last_name AS student_last_name, \
        groups.id AS group_id, \
        groups.name AS group_name \
    FROM students AS students \
        LEFT JOIN groups AS groups \
        ON students.group_id = groups.id \
    WHERE students.id = :id
    
students.select.byFullName = \
    SELECT \
        students.id AS student_id, \
        students.first_name AS student_first_name, \
        students.last_name AS student_last_name, \
        groups.id AS group_id, \
        groups.name AS group_name \
    FROM students AS students \
        LEFT JOIN groups AS groups \
        ON students.group_id = groups.id \
    WHERE students.first_Name = :firstName \
        AND students.last_name = :lastName
        
students.select.byGroupId = \
    SELECT \
        students.id AS student_id, \
        students.first_name AS student_first_name, \
        students.last_name AS student_last_name, \
        groups.id AS group_id, \
        groups.name AS group_name \
    FROM students AS students \
        LEFT JOIN groups AS groups \
        ON students.group_id = groups.id \
    WHERE groups.id = :groupId
                
students.insert = \
    INSERT INTO students (first_name, last_name, group_id) \
    values (:firstName, :lastName, :groupId)
students.update = \
    UPDATE students \
    SET first_name = :firstName, \
    last_name = :lastName, \
    group_id = :groupId \
    WHERE students.id = :id

teachers.select = \
    SELECT \
        id AS teacher_id, \
        first_name AS teacher_first_name, \
        last_name AS teacher_last_name \
     FROM \
        teachers
        
teachers.select.byId = \
    SELECT \
        id AS teacher_id, \
        first_name AS teacher_first_name, \
        last_name AS teacher_last_name \
     FROM \
        teachers \
     WHERE \
        id = :id
        
teachers.select.byFullName = \
    SELECT \
        id AS teacher_id, \
        first_name AS teacher_first_name, \
        last_name AS teacher_last_name \
     FROM \
        teachers \
     WHERE \
        first_Name = :firstName \
        AND last_name = :lastName
        
teachers.insert = \
    INSERT INTO teachers (first_name, last_name) \
    values (:firstName, :lastName)
teachers.update = \
    UPDATE teachers \
    SET first_name = :firstName, \
    last_name = :lastName \
    WHERE teachers.id = :id

teachers_courses.select = \
    SELECT \
        teachers_courses.teacher_id AS teacher_id, \
        teachers_courses.course_id AS course_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name, \
        courses.name AS course_name \
    FROM \
        teachers_courses AS teachers_courses \
            LEFT JOIN teachers AS teachers \
            ON teachers_courses.teacher_id = teachers.id \
            LEFT JOIN courses AS courses \
            ON teachers_courses.course_id = courses.id
teachers_courses.select.byTeacherId = \
    SELECT \
        teachers_courses.teacher_id AS teacher_id, \
        teachers_courses.course_id AS course_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name, \
        courses.name AS course_name \
    FROM \
        teachers_courses AS teachers_courses \
            LEFT JOIN teachers AS teachers \
            ON teachers_courses.teacher_id = teachers.id \
            LEFT JOIN courses AS courses \
            ON teachers_courses.course_id = courses.id \
    WHERE \
        teachers.id = :id
        
teachers_courses.select.byCourseId = \
    SELECT \
        teachers_courses.teacher_id AS teacher_id, \
        teachers_courses.course_id AS course_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name, \
        courses.name AS course_name \
    FROM \
        teachers_courses AS teachers_courses \
            LEFT JOIN teachers AS teachers \
            ON teachers_courses.teacher_id = teachers.id \
            LEFT JOIN courses AS courses \
            ON teachers_courses.course_id = courses.id \
    WHERE \
        course_id = :id        

teachers_courses.insert = \
    INSERT INTO teachers_courses (teacher_id, course_id) \
    VALUES (:teacher_id, :course_id)    
teachers_courses.delete = DELETE FROM teachers_courses
teachers_courses.delete.byTeacherIdAndCourseId = DELETE FROM teachers_courses \                   
    
groups.select = \
    SELECT \
        id AS group_id, \
        name AS group_name \
    FROM groups
    
groups.select.byId = \
    SELECT \
        id AS group_id, \
        name AS group_name \
    FROM groups \
    WHERE groups.id = :id
    
groups.select.byName = \
    SELECT \
        id AS group_id, \
        name AS group_name \
    FROM groups \
    WHERE groups.name = :name
    
groups.insert = \
    INSERT INTO groups (name) \
    values (:name)
    
groups.update = \
    UPDATE groups \
    SET name = :name \
    WHERE groups.id = :id    

groups_courses.select = \
    SELECT \
        groups_courses.group_id AS group_id, \
        groups_courses.course_id AS course_id, \
        groups.name AS group_name, \
        courses.name AS course_name \
    FROM \
        groups_courses AS groups_courses \
            LEFT JOIN groups AS groups \
            ON groups_courses.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON groups_courses.course_id = courses.id
            
groups_courses.select.byCourseId = \
    SELECT \
        groups_courses.group_id AS group_id, \
        groups_courses.course_id AS course_id, \
        groups.name AS group_name, \
        courses.name AS course_name \
    FROM \
        groups_courses AS groups_courses \
            LEFT JOIN groups AS groups \
            ON groups_courses.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON groups_courses.course_id = courses.id \
    WHERE course_id = :id
    
groups_courses.select.byGroupId = \
    SELECT \
        groups_courses.group_id AS group_id, \
        groups_courses.course_id AS course_id, \
        groups.name AS group_name, \
        courses.name AS course_name \
    FROM \
        groups_courses AS groups_courses \
            LEFT JOIN groups AS groups \
            ON groups_courses.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON groups_courses.course_id = courses.id \
    WHERE group_id = :id
            
groups_courses.insert = \
    INSERT INTO groups_courses (group_id, course_id) \
    VALUES (:groupId, :courseId)
    
groups_courses.delete = DELETE FROM groups_courses
groups_courses.delete.byGroupIdAndCourseId = \
    DELETE FROM groups_courses \
    WHERE group_id = :groupId \
        AND course_id = :courseId
    
courses.select = \
    SELECT \
        id AS course_id, name AS course_name \
    FROM courses

courses.select.byId = \
    SELECT \
        id AS course_id, name AS course_name \
    FROM courses \
    WHERE id = :id
courses.select.byName = \
    SELECT \
        id AS course_id, name AS course_name \
    FROM courses \
    WHERE name = :name

courses.insert = \
    INSERT INTO courses (name) \
    values (:name)
courses.update = \
    UPDATE courses \
    SET name = :name \
    WHERE courses.id = :id
    
tabletime.select = \
    SELECT \
        tabletime.date_time AS tabletime_datetime, \
        tabletime.group_id AS group_id, \
        groups.name AS group_name, \
        tabletime.course_id AS course_id, \
        courses.name AS course_name, \
        tabletime.teacher_id AS teacher_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name \
    FROM \
        tabletime AS tabletime \
            LEFT JOIN groups AS groups \
            ON tabletime.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON tabletime.course_id = courses.id \
            LEFT JOIN teachers AS teachers \
            ON tabletime.teacher_id = teachers.id
            
tabletime.select.byPeriodAndTeacherId = \
    SELECT \
        tabletime.date_time AS tabletime_datetime, \
        tabletime.group_id AS group_id, \
        groups.name AS group_name, \
        tabletime.course_id AS course_id, \
        courses.name AS course_name, \
        tabletime.teacher_id AS teacher_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name \
    FROM \
        tabletime AS tabletime \
            LEFT JOIN groups AS groups \
            ON tabletime.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON tabletime.course_id = courses.id \
            LEFT JOIN teachers AS teachers \
            ON tabletime.teacher_id = teachers.id \
    WHERE \
        tabletime.date_time BETWEEN :begin AND :end \
        AND tabletime.teacher_id = :teacherId 
        
tabletime.select.byPeriodAndCourseId = \
    SELECT \
        tabletime.date_time AS tabletime_datetime, \
        tabletime.group_id AS group_id, \
        groups.name AS group_name, \
        tabletime.course_id AS course_id, \
        courses.name AS course_name, \
        tabletime.teacher_id AS teacher_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name \
    FROM \
        tabletime AS tabletime \
            LEFT JOIN groups AS groups \
            ON tabletime.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON tabletime.course_id = courses.id \
            LEFT JOIN teachers AS teachers \
            ON tabletime.teacher_id = teachers.id \
    WHERE \
        tabletime.date_time BETWEEN :begin AND :end \
        AND tabletime.course_id = :courseId
        
tabletime.select.byPeriodAndGroupId = \
    SELECT \
        tabletime.date_time AS tabletime_datetime, \
        tabletime.group_id AS group_id, \
        groups.name AS group_name, \
        tabletime.course_id AS course_id, \
        courses.name AS course_name, \
        tabletime.teacher_id AS teacher_id, \
        teachers.first_name AS teacher_first_name, \
        teachers.last_name AS teacher_last_name \
    FROM \
        tabletime AS tabletime \
            LEFT JOIN groups AS groups \
            ON tabletime.group_id = groups.id \
            LEFT JOIN courses AS courses \
            ON tabletime.course_id = courses.id \
            LEFT JOIN teachers AS teachers \
            ON tabletime.teacher_id = teachers.id \
    WHERE \
        tabletime.date_time BETWEEN :begin AND :end \
        AND tabletime.group_id = :groupId

tabletime.insert =  \
    INSERT INTO tabletime (date_time, group_id, course_id, teacher_id) \
    VALUES (:dateTime, :groupId, :courseId, :teacherId)

tabletime.update = \
    UPDATE tabletime \
    SET date_time = :dateTime, \
    group_id = :groupId, \
    course_id = :courseId, \
    teacher_id = :teacherId
    
tabletime.update.byTeacherId = \
    UPDATE tabletime \
    SET date_time = :dateTime, \
    group_id = :groupId, \
    course_id = :courseId, \
    teacher_id = :teacherId \
    WHERE teacher_id = :teacherId
    
tabletime.update.byCourseId = \
    UPDATE tabletime \
    SET date_time = :dateTime, \
    group_id = :groupId, \
    course_id = :courseId, \
    teacher_id = :teacherId \
    WHERE course_id = :courseId
    
tabletime.update.byGroupId = \
    UPDATE tabletime \
    SET date_time = :dateTime, \
    group_id = :groupId, \
    course_id = :courseId, \
    teacher_id = :teacherId \
    WHERE group_id = :groupId
    